# `aurelia-nhsuk-components`

## Using the plugin

```ts
aurelia.use.plugin('aurelia-nhsuk-components');

// for webpack users, use the PLATFORM.moduleName wrapper
aurelia.use.plugin(PLATFORM.moduleName('aurelia-nhsuk-components'));
```
